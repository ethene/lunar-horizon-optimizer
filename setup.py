from setuptools import setup, find_packages

setup(
    name="lunar-horizon-optimizer",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        'numpy',
        'pytest',
    ],
) 